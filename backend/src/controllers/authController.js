const register = async (req, res) => res.json({ success: true, message: "Register endpoint placeholder" });
const login = async (req, res) => res.json({ success: true, message: "Login endpoint placeholder" });

module.exports = { register, login };
